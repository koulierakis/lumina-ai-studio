(()=>{
  'use strict';
  const RADII=[1200,2500,5000,10000];
  const ENDPOINTS=['https://overpass-api.de/api/interpreter','https://overpass.kumi.systems/api/interpreter','https://overpass.openstreetmap.fr/api/interpreter','https://overpass.private.coffee/api/interpreter','https://maps.mail.ru/osm/tools/overpass/api/interpreter'];
  const PHOTON='https://photon.komoot.io/api/';
  const $=s=>document.querySelector(s),results=$('#poiResults');
  const rad=v=>v*Math.PI/180;
  const dist=(a,b)=>{const R=6371e3,p1=rad(a.lat),p2=rad(b.lat),dp=rad(b.lat-a.lat),dl=rad(b.lng-a.lng),x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(x),Math.sqrt(1-x))};
  const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9α-ω]+/gi,' ').trim();
  const fd=m=>m<1000?`${Math.round(m)} m`:`${(m/1000).toFixed(1)} km`;
  const delay=ms=>new Promise(r=>setTimeout(r,ms));
  const tagsByType={restaurant:[['amenity','restaurant'],['amenity','fast_food']],fast_food:[['amenity','fast_food'],['amenity','restaurant']],cafe:[['amenity','cafe'],['shop','coffee']],bar:[['amenity','bar'],['amenity','pub']],ice_cream:[['amenity','ice_cream'],['shop','ice_cream']],hotel:[['tourism','hotel']],motel:[['tourism','motel']],guest_house:[['tourism','guest_house']],camp_site:[['tourism','camp_site']],supermarket:[['shop','supermarket']],convenience:[['shop','convenience']],bakery:[['shop','bakery']],mall:[['shop','mall']],pharmacy:[['amenity','pharmacy']],hospital:[['amenity','hospital']],clinic:[['amenity','clinic']],dentist:[['amenity','dentist']],fuel:[['amenity','fuel']],parking:[['amenity','parking']],charging:[['amenity','charging_station']],taxi:[['amenity','taxi']],bus:[['highway','bus_stop']],bank:[['amenity','bank']],atm:[['amenity','atm']],police:[['amenity','police']],post:[['amenity','post_office']],gym:[['leisure','fitness_centre']],beach:[['natural','beach']],museum:[['tourism','museum']],attraction:[['tourism','attraction']],playground:[['leisure','playground']]};
  const terms={restaurant:['restaurant','taverna','ταβέρνα','εστιατόριο'],fast_food:['fast food','souvlaki','σουβλάκι'],cafe:['cafe','coffee','καφέ'],bar:['bar','pub'],ice_cream:['ice cream','gelato','παγωτό'],hotel:['hotel'],motel:['motel'],guest_house:['guest house'],camp_site:['camping'],supermarket:['supermarket'],convenience:['mini market'],bakery:['bakery'],mall:['shopping mall'],pharmacy:['pharmacy'],hospital:['hospital'],clinic:['clinic'],dentist:['dentist'],fuel:['fuel'],parking:['parking'],charging:['charging station'],taxi:['taxi'],bus:['bus stop'],bank:['bank'],atm:['atm'],police:['police'],post:['post office'],gym:['gym'],beach:['beach'],museum:['museum'],attraction:['attraction'],playground:['playground']};
  function sharedFix(){try{const p=window.LuminaGPS?.getLastFix?.(45000)||window.LuminaGPS?.lastFix;if(Number.isFinite(+p?.lat)&&Number.isFinite(+p?.lng))return{lat:+p.lat,lng:+p.lng,accuracy:p.accuracy||null,source:p.source||'shared-gps'}}catch{}return null}
  function markerPosition(){try{const p=window.__luminaCurrentUserMarker?.getLatLng?.();if(Number.isFinite(p?.lat)&&Number.isFinite(p?.lng))return{lat:+p.lat,lng:+p.lng,source:'active-map'}}catch{}return null}
  function oneGpsAttempt(timeout=7000){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('gps-unavailable'));navigator.geolocation.getCurrentPosition(p=>{const lat=Number(p.coords.latitude),lng=Number(p.coords.longitude);if(!Number.isFinite(lat)||!Number.isFinite(lng))return reject(new Error('gps-invalid'));resolve({lat,lng,accuracy:Number(p.coords.accuracy)||null,source:'geolocation'})},reject,{enableHighAccuracy:true,timeout,maximumAge:5000})})}
  async function gps(){const shared=sharedFix();if(shared)return shared;const marker=markerPosition();if(marker)return marker;if(window.LuminaGPS?.getPosition){try{return await window.LuminaGPS.getPosition({maxAge:45000,timeout:8000})}catch{}}let lastError;for(let attempt=1;attempt<=2;attempt++){try{return await oneGpsAttempt(6500+attempt*1000)}catch(e){lastError=e;if(attempt<2)await delay(400)}}throw lastError||new Error('gps-failed')}
  async function json(url,opts={},timeout=16000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{...opts,signal:c.signal,cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return await r.json()}finally{clearTimeout(t)}}
  function point(el){if(Number.isFinite(el.lat)&&Number.isFinite(el.lon))return{lat:el.lat,lng:el.lon};if(Number.isFinite(el.center?.lat)&&Number.isFinite(el.center?.lon))return{lat:el.center.lat,lng:el.center.lon};return null}
  function overpassQuery(type,o,radius){const clauses=[];for(const [k,v] of tagsByType[type]||[])for(const kind of ['node','way','relation'])clauses.push(`${kind}(around:${radius},${o.lat},${o.lng})["${k}"="${v}"];`);return`[out:json][timeout:25];(${clauses.join('')});out center tags;`}
  async function overpass(type,o,radius){const q=overpassQuery(type,o,radius);let last;for(const ep of ENDPOINTS){try{const d=await json(`${ep}?data=${encodeURIComponent(q)}`,{headers:{Accept:'application/json'}},22000);return(d.elements||[]).map(el=>{const p=point(el),t=el.tags||{};if(!p)return null;const name=t['name:el']||t.name||t.brand||t.operator;if(!name)return null;return{name,point:p,distance:dist(o,p),area:[t['addr:street'],t['addr:place']||t['addr:city']||t['addr:town']||t['addr:village']].filter(Boolean).join(' · ')||'Κοντινή περιοχή',source:'OSM'}}).filter(x=>x&&x.distance<=radius)}catch(e){last=e}}throw last||new Error('overpass')}
  async function photon(type,o,radius){const accepted=new Set((tagsByType[type]||[]).map(([k,v])=>`${k}:${v}`)),out=[];for(const term of (terms[type]||[type]).slice(0,4)){try{const u=new URL(PHOTON);u.searchParams.set('q',term);u.searchParams.set('lat',o.lat);u.searchParams.set('lon',o.lng);u.searchParams.set('limit','50');u.searchParams.set('lang','el');const d=await json(u.toString(),{headers:{Accept:'application/json'}},10000);for(const f of d.features||[]){const p=f.properties||{},c=f.geometry?.coordinates||[],pt={lat:+c[1],lng:+c[0]},key=`${p.osm_key||''}:${p.osm_value||''}`;if(!accepted.has(key)||!Number.isFinite(pt.lat)||!Number.isFinite(pt.lng))continue;const name=p.name||p.street||p.city;if(!name)continue;const distance=dist(o,pt);if(distance>radius)continue;out.push({name,point:pt,distance,area:[p.street,p.city||p.town||p.village].filter(Boolean).join(' · ')||'Κοντινή περιοχή',source:'Photon'})}}catch{}}return out}
  function merge(items){const seen=new Set(),out=[];for(const x of items.flat().sort((a,b)=>a.distance-b.distance)){const k=`${norm(x.name)}|${x.point.lat.toFixed(4)}|${x.point.lng.toFixed(4)}`;if(seen.has(k))continue;seen.add(k);out.push(x)}return out}
  function render(items,radius,o,provider){if(!results)return;if(!items.length){results.innerHTML='<div class="poi-loading">Δεν βρέθηκαν καταχωρημένα σημεία αυτής της κατηγορίας έως 10 km από την πραγματική θέση GPS.</div>';return}const nearest=items[0];results.innerHTML=`<div class="poi-loading"><strong>${items.length} σημεία</strong> · από το κοντινότερο · ${esc(provider||'Nearby')}<br><small>Κοντινότερο: ${esc(nearest.name)} · ${fd(nearest.distance)} · GPS${o.accuracy?` ±${Math.round(o.accuracy)}m`:''}</small></div>`+items.slice(0,80).map((x,i)=>`<article class="poi-result" data-rank="${i+1}"><div><strong>${i+1}. ${esc(x.name)}</strong><span>${fd(x.distance)} · ${esc(x.area||x.address||'Κοντινή περιοχή')}</span></div><button type="button" class="poi-route" data-lat="${x.point?.lat??x.lat}" data-lng="${x.point?.lng??x.lng}" data-name="${esc(x.name)}">▶ Έναρξη</button></article>`).join('')}
  async function googleFirst(type,o){if(!window.LuminaGooglePlaces?.hasKey?.())return null;try{results.innerHTML=`<div class="poi-loading busy"><span class="poi-spinner"></span><strong>Google Places: βρίσκω τα πραγματικά κοντινότερα…</strong><small>GPS ${o.accuracy?`±${Math.round(o.accuracy)}m`:''}</small></div>`;const {items,radius}=await Promise.race([window.LuminaGooglePlaces.nearbyProgressive(type,{lat:o.lat,lng:o.lng}),new Promise((_,rej)=>setTimeout(()=>rej(new Error('google-nearby-timeout')),12000))]);if(items?.length){const mapped=items.map(x=>({name:x.name,point:{lat:x.lat,lng:x.lng},distance:x.distance,area:x.address||'Κοντινή περιοχή',source:'Google Places'}));render(mapped,radius,o,'Google Places');return true}}catch(e){console.warn('[LUMINA Google Nearby fallback]',e)}return false}
  async function run(type){if(!results)return;results.innerHTML='<div class="poi-loading busy"><span class="poi-spinner"></span><strong>Κλειδώνω την πραγματική θέση GPS…</strong><small>Δεν χρησιμοποιώ σταθερό σημείο ή κέντρο χάρτη.</small></div>';let o;try{o=await gps()}catch{results.innerHTML='<div class="poi-loading error">Δεν υπάρχει διαθέσιμο GPS fix. Περίμενε να εμφανιστεί ακρίβεια GPS και ξαναπάτησε την κατηγορία.</div>';return}if(await googleFirst(type,o))return;let final=[],usedRadius=RADII[RADII.length-1],anyProvider=false;for(const radius of RADII){results.innerHTML=`<div class="poi-loading busy"><span class="poi-spinner"></span><strong>Google δεν απάντησε — χρησιμοποιώ εφεδρικές πηγές…</strong><small>${fd(radius)} γύρω από GPS ${o.accuracy?`±${Math.round(o.accuracy)}m`:''}</small></div>`;const settled=await Promise.allSettled([overpass(type,o,radius),photon(type,o,radius)]);const good=settled.filter(x=>x.status==='fulfilled').map(x=>x.value);if(good.length)anyProvider=true;const found=merge(good);if(found.length){final=found;usedRadius=radius;if(found.length>=8||radius===RADII[RADII.length-1])break}}
    if(!anyProvider){results.innerHTML='<div class="poi-loading error">Το GPS λειτουργεί, αλλά καμία υπηρεσία επιχειρήσεων/POI δεν απάντησε.</div>';return}render(final,usedRadius,o,'OSM fallback')}
  document.addEventListener('click',e=>{const b=e.target.closest?.('[data-poi-type]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();run(b.dataset.poiType)},true);
})();