name: LUMINA Drive Assistant

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Drive Pages deployment moved to feature/lumina-drive-assistant"
