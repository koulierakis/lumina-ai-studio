name: LUMINA Drive Assistant Legacy Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm canonical deployment branch
        run: |
          echo "Drive Pages deployment is owned exclusively by feature/lumina-drive-assistant."
          echo "This legacy/default branch workflow cannot deploy GitHub Pages."
