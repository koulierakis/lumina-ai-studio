name: LUMINA Drive Legacy Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical deployment notice
        run: |
          echo "GitHub Pages deployment is owned exclusively by feature/lumina-drive-assistant."
          echo "This default-branch workflow intentionally cannot deploy Pages."
