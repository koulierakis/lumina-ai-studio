(()=>{
  const $=s=>document.querySelector(s);
  const CATEGORIES={
    stay:{label:'Διαμονή',items:[['hotel','🏨 Ξενοδοχεία','tourism','hotel'],['motel','🛏️ Μοτέλ','tourism','motel'],['guest_house','🏡 Ξενώνες','tourism','guest_house'],['camp_site','⛺ Camping','tourism','camp_site']]},
    food:{label:'Φαγητό & Ποτό',items:[['restaurant','🍽️ Εστιατόρια','amenity','restaurant'],['fast_food','🍔 Fast food / Σουβλάκι','amenity','fast_food'],['cafe','☕ Καφέ','amenity','cafe'],['bar','🍸 Bar','amenity','bar'],['ice_cream','🍦 Παγωτό','amenity','ice_cream']]},
    shopping:{label:'Αγορές',items:[['supermarket','🛒 Supermarket','shop','supermarket'],['convenience','🥤 Mini market','shop','convenience'],['bakery','🥖 Φούρνοι','shop','bakery'],['mall','🏬 Εμπορικά κέντρα','shop','mall']]},
    health:{label:'Υγεία',items:[['pharmacy','💊 Φαρμακεία','amenity','pharmacy'],['hospital','🏥 Νοσοκομεία','amenity','hospital'],['clinic','🩺 Κλινικές','amenity','clinic'],['dentist','🦷 Οδοντίατροι','amenity','dentist']]},
    mobility:{label:'Μετακίνηση',items:[['fuel','⛽ Βενζινάδικα','amenity','fuel'],['parking','🅿️ Parking','amenity','parking'],['charging','⚡ Φόρτιση EV','amenity','charging_station'],['taxi','🚕 Taxi','amenity','taxi'],['bus','🚌 Στάσεις λεωφορείου','highway','bus_stop']]},
    services:{label:'Υπηρεσίες',items:[['bank','🏦 Τράπεζες','amenity','bank'],['atm','💶 ATM','amenity','atm'],['police','👮 Αστυνομία','amenity','police'],['post','📮 Ταχυδρομεία','amenity','post_office']]},
    leisure:{label:'Αναψυχή',items:[['gym','🏋️ Γυμναστήρια','leisure','fitness_centre'],['beach','🏖️ Παραλίες','natural','beach'],['museum','🏛️ Μουσεία','tourism','museum'],['attraction','📸 Αξιοθέατα','tourism','attraction'],['playground','🛝 Παιδικές χαρές','leisure','playground']]}
  };
  const KEYWORDS={pharmacy:'pharmacy',hospital:'hospital',clinic:'clinic',dentist:'dentist',fuel:'fuel',parking:'parking',charging:'charging station',taxi:'taxi',bus:'bus stop',restaurant:'restaurant',fast_food:'fast food',cafe:'cafe',bar:'bar',ice_cream:'ice cream',hotel:'hotel',motel:'motel',guest_house:'guest house',camp_site:'camping',supermarket:'supermarket',convenience:'mini market',bakery:'bakery',mall:'shopping mall',bank:'bank',atm:'atm',police:'police',post:'post office',gym:'gym',beach:'beach',museum:'museum',attraction:'attraction',playground:'playground'};
  const OVERPASS_ENDPOINTS=['https://overpass-api.de/api/interpreter','https://overpass.kumi.systems/api/interpreter'];
  const SEARCH_RADII=[1500,3000,6000,12000];
  const drawer=$('#poiDrawer'),cats=$('#poiCategories'),results=$('#poiResults'),title=$('#poiTitle'),back=$('#poiBackBtn');
  function open(){drawer.classList.remove('hidden');renderCategories()}
  function close(){drawer.classList.add('hidden')}
  function renderCategories(){title.textContent='Σημεία ενδιαφέροντος';back.classList.add('hidden');results.innerHTML='';cats.innerHTML=Object.entries(CATEGORIES).map(([k,v])=>`<button class="poi-category" data-poi-group="${k}"><strong>${v.label}</strong><span>${v.items.length} κατηγορίες</span></button>`).join('')}
  function renderGroup(key){const g=CATEGORIES[key];if(!g)return;title.textContent=g.label;back.classList.remove('hidden');cats.innerHTML=g.items.map(i=>`<button class="poi-item" data-poi-type="${i[0]}" data-k="${i[2]}" data-v="${i[3]}">${i[1]}</button>`).join('');results.innerHTML=''}
  function getPos(){return new Promise((res,rej)=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>res({lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy}),rej,{enableHighAccuracy:true,maximumAge:2000,timeout:15000}):rej(new Error('geolocation-unavailable')))}
  const esc=s=>String(s||'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
  const rad=v=>v*Math.PI/180;
  function distance(a,b){const R=6371e3,p1=rad(a.lat),p2=rad(b.lat),dp=rad(b.lat-a.lat),dl=rad(b.lng-a.lng),x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(x),Math.sqrt(1-x))}
  const fd=m=>m<1000?`${Math.round(m)} m`:`${(m/1000).toFixed(1)} km`;
  function boxFor(c,radius){const latDelta=radius/111320,lonDelta=radius/(111320*Math.max(.25,Math.cos(rad(c.lat))));return`${c.lng-lonDelta},${c.lat+latDelta},${c.lng+lonDelta},${c.lat-latDelta}`}
  async function fetchJson(url,opts={},timeout=7000){const ctl=new AbortController(),t=setTimeout(()=>ctl.abort(),timeout);try{const r=await fetch(url,{...opts,signal:ctl.signal,headers:{Accept:'application/json',...(opts.headers||{})}});if(!r.ok)throw new Error(`http-${r.status}`);return await r.json()}finally{clearTimeout(t)}}
  async function fetchOverpass(query){let lastError;for(const endpoint of OVERPASS_ENDPOINTS){try{return await fetchJson(endpoint,{method:'POST',body:`data=${encodeURIComponent(query)}`,headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'}},7500)}catch(e){lastError=e}}throw lastError||new Error('overpass-unavailable')}
  function makeQuery(c,k,v,radius){return `[out:json][timeout:9];(node(around:${radius},${c.lat},${c.lng})[\"${k}\"=\"${v}\"];way(around:${radius},${c.lat},${c.lng})[\"${k}\"=\"${v}\"];relation(around:${radius},${c.lat},${c.lng})[\"${k}\"=\"${v}\"];);out center tags 100;`}
  function fromOverpass(j,c,k,v,radius){const arr=[];for(const x of(j.elements||[])){const tags=x.tags||{};if(tags[k]!==v)continue;const p=Number.isFinite(x.lat)?{lat:x.lat,lng:x.lon}:x.center&&Number.isFinite(x.center.lat)?{lat:x.center.lat,lng:x.center.lon}:null;if(!p)continue;const d=distance(c,p);if(d>radius)continue;arr.push({p,d,name:tags.name||tags.brand||tags.operator||'Χωρίς καταχωρημένο όνομα',address:[tags['addr:street'],tags['addr:housenumber'],tags['addr:city']||tags['addr:place']].filter(Boolean).join(' '),source:'overpass'})}return arr}
  async function searchPhoton(type,k,v,c,radius){const term=KEYWORDS[type]||v,u=new URL('https://photon.komoot.io/api/');u.searchParams.set('q',term);u.searchParams.set('lat',String(c.lat));u.searchParams.set('lon',String(c.lng));u.searchParams.set('limit','50');u.searchParams.set('lang','el');u.searchParams.set('osm_tag',`${k}:${v}`);const j=await fetchJson(u.toString(),{},7000),out=[];for(const f of(j.features||[])){const pr=f.properties||{},co=f.geometry?.coordinates||[];if(pr.osm_key!==k||pr.osm_value!==v)continue;const p={lat:+co[1],lng:+co[0]};if(!Number.isFinite(p.lat)||!Number.isFinite(p.lng))continue;const d=distance(c,p);if(d>radius)continue;out.push({p,d,name:pr.name||pr.street||term,address:[pr.street,pr.housenumber,pr.city||pr.town||pr.village].filter(Boolean).join(' '),source:'photon'})}return out}
  async function searchNominatim(type,k,v,c,radius){const u=new URL('https://nominatim.openstreetmap.org/search');u.searchParams.set('format','jsonv2');u.searchParams.set('q',KEYWORDS[type]||v);u.searchParams.set('countrycodes','gr');u.searchParams.set('limit','50');u.searchParams.set('addressdetails','1');u.searchParams.set('namedetails','1');u.searchParams.set('extratags','1');u.searchParams.set('bounded','1');u.searchParams.set('viewbox',boxFor(c,radius));const j=await fetchJson(u.toString(),{},7500),out=[];for(const x of(j||[])){if(x.category!==k||x.type!==v)continue;const p={lat:+x.lat,lng:+x.lon};if(!Number.isFinite(p.lat)||!Number.isFinite(p.lng))continue;const d=distance(c,p);if(d>radius)continue;const a=x.address||{};out.push({p,d,name:x.namedetails?.name||x.name||String(x.display_name||'').split(',')[0]||KEYWORDS[type]||v,address:[a.road,a.house_number,a.village||a.town||a.city||a.municipality].filter(Boolean).join(' '),source:'nominatim'})}return out}
  function merge(items){const seen=new Set(),out=[];for(const x of items.sort((a,b)=>a.d-b.d)){const key=`${Math.round(x.p.lat*1e5)}:${Math.round(x.p.lng*1e5)}:${String(x.name).toLowerCase()}`;if(seen.has(key))continue;seen.add(key);out.push(x)}return out}
  function renderResults(arr,radius){const note=`<div class="poi-loading">Από το κοντινότερο · ακτίνα ${radius<1000?radius+' m':radius/1000+' km'}</div>`;results.innerHTML=arr.length?note+arr.slice(0,30).map(x=>`<article class="poi-result"><div><strong>${esc(x.name)}</strong><span>${fd(x.d)}${x.address?' · '+esc(x.address):''}</span></div><button class="poi-route" data-lat="${x.p.lat}" data-lng="${x.p.lng}" data-name="${esc(x.name)}">Οδηγίες</button></article>`).join(''):'<div class="poi-loading">Δεν βρέθηκαν καταχωρημένα σημεία αυτής της κατηγορίας κοντά σου.</div>'}
  async function searchTier(type,k,v,c,radius){const jobs=[(async()=>{try{return fromOverpass(await fetchOverpass(makeQuery(c,k,v,radius)),c,k,v,radius)}catch{return[]}})(),(async()=>{try{return await searchPhoton(type,k,v,c,radius)}catch{return[]}})(),(async()=>{try{return await searchNominatim(type,k,v,c,radius)}catch{return[]}})()];const sets=await Promise.all(jobs);return merge(sets.flat()).filter(x=>x.d<=radius)}
  async function search(type,k,v){results.innerHTML='<div class="poi-loading">Εντοπίζω τα πιο κοντινά σημεία…</div>';try{const c=await getPos();let chosen=[],usedRadius=SEARCH_RADII.at(-1);for(const radius of SEARCH_RADII){const tier=await searchTier(type,k,v,c,radius);if(tier.length){chosen=tier;usedRadius=radius;if(tier.length>=5||radius===SEARCH_RADII.at(-1))break}}renderResults(chosen,usedRadius)}catch(e){const gpsProblem=e&&(/geolocation|position|permission/i.test(String(e.message||e))||e.code);results.innerHTML=`<div class="poi-loading">${gpsProblem?'Δεν ήταν διαθέσιμη η ακριβής θέση GPS.':'Δεν ήταν δυνατή η αναζήτηση κοντινών σημείων. Δοκίμασε ξανά.'}</div>`}}
  $('#poiOpenBtn')?.addEventListener('click',open);$('#poiCloseBtn')?.addEventListener('click',close);back?.addEventListener('click',renderCategories);
  cats?.addEventListener('click',e=>{const g=e.target.closest('[data-poi-group]');if(g)return renderGroup(g.dataset.poiGroup);const i=e.target.closest('[data-k]');if(i)search(i.dataset.poiType,i.dataset.k,i.dataset.v)});
  results?.addEventListener('click',e=>{if(e.target.closest('.poi-route'))close()});
})();