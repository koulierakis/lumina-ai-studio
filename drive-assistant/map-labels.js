(()=>{
  if(!window.L||!L.map||L.map.__luminaWrapped)return;
  const originalMap=L.map.bind(L);
  const ENDPOINTS=['https://overpass-api.de/api/interpreter','https://overpass.kumi.systems/api/interpreter','https://overpass.openstreetmap.fr/api/interpreter','https://overpass.private.coffee/api/interpreter','https://maps.mail.ru/osm/tools/overpass/api/interpreter'];
  const wrappedMap=function(...args){
    const map=originalMap(...args);window.__luminaDriveMap=map;
    map.createPane('roadNames');const pane=map.getPane('roadNames');pane.style.zIndex='470';pane.style.pointerEvents='none';
    const labelsLayer=L.layerGroup([],{pane:'roadNames'}).addTo(map);let currentRoadLabel=null,lastKey='',loading=false,timer=null;
    if(!document.getElementById('lumina-road-label-style')){const style=document.createElement('style');style.id='lumina-road-label-style';style.textContent='.lumina-road-name.leaflet-tooltip,.lumina-current-road.leaflet-tooltip{background:rgba(255,255,255,.94);border:1px solid rgba(20,35,55,.16);color:#172235;border-radius:7px;padding:3px 6px;font-size:11px;font-weight:850;line-height:1.1;box-shadow:0 2px 7px rgba(0,0,0,.16);white-space:nowrap}.lumina-current-road.leaflet-tooltip{background:rgba(7,17,31,.94);color:#fff;border-color:rgba(88,221,209,.55);font-size:12px}.lumina-road-name.leaflet-tooltip:before,.lumina-current-road.leaflet-tooltip:before{display:none}';document.head.appendChild(style)}
    const updateCurrentRoad=()=>{const marker=window.__luminaCurrentUserMarker,road=document.getElementById('roadName')?.textContent?.trim();if(!marker||!road||road==='—')return;const latlng=marker.getLatLng?.();if(!latlng)return;if(currentRoadLabel)labelsLayer.removeLayer(currentRoadLabel);currentRoadLabel=L.circleMarker(latlng,{pane:'roadNames',radius:1,opacity:0,fillOpacity:0,interactive:false}).addTo(labelsLayer);currentRoadLabel.bindTooltip(road,{permanent:true,direction:'top',offset:[0,-14],className:'lumina-current-road',opacity:1})};
    async function fetchRoads(q){let last;for(const ep of ENDPOINTS){try{const c=new AbortController(),t=setTimeout(()=>c.abort(),12000);const r=await fetch(ep,{method:'POST',body:q,signal:c.signal});clearTimeout(t);if(!r.ok)throw new Error(`HTTP ${r.status}`);return await r.json()}catch(e){last=e}}throw last||new Error('roads')}
    function midpoint(geom){if(!Array.isArray(geom)||!geom.length)return null;const idx=Math.floor((geom.length-1)/2),p=geom[idx];return p&&Number.isFinite(p.lat)&&Number.isFinite(p.lon)?[p.lat,p.lon]:null}
    const loadRoadNames=async()=>{if(map.getZoom()<13||loading){if(map.getZoom()<13){labelsLayer.clearLayers();currentRoadLabel=null;lastKey=''}return}const b=map.getBounds().pad(.12),c=b.getCenter(),key=`${map.getZoom()}:${c.lat.toFixed(3)}:${c.lng.toFixed(3)}`;if(key===lastKey){updateCurrentRoad();return}loading=true;try{const s=b.getSouth(),w=b.getWest(),n=b.getNorth(),e=b.getEast(),q=`[out:json][timeout:12];way(${s},${w},${n},${e})[highway][name];out tags geom 120;`,j=await fetchRoads(q);labelsLayer.clearLayers();currentRoadLabel=null;const candidates=[];for(const way of j.elements||[]){const name=way.tags?.['name:el']||way.tags?.name,geom=way.geometry;if(!name||!Array.isArray(geom)||geom.length<2)continue;const ref=way.tags?.ref,label=ref&&!name.includes(ref)?`${name} · ${ref}`:name,mid=midpoint(geom);if(!mid)continue;const d=map.distance(c,L.latLng(mid[0],mid[1]));candidates.push({label,mid,d,type:way.tags?.highway||''})}const rank={motorway:0,trunk:1,primary:2,secondary:3,tertiary:4,residential:5,unclassified:6,living_street:7,service:8};candidates.sort((a,b)=>(rank[a.type]??9)-(rank[b.type]??9)||a.d-b.d);const seen=new Set(),used=[];for(const x of candidates){const keyName=x.label.toLocaleLowerCase('el-GR');if(seen.has(keyName)&&map.getZoom()<16)continue;let tooClose=false;for(const p of used){if(map.distance(L.latLng(x.mid[0],x.mid[1]),L.latLng(p[0],p[1]))<85){tooClose=true;break}}if(tooClose)continue;seen.add(keyName);used.push(x.mid);const marker=L.circleMarker(x.mid,{pane:'roadNames',radius:1,opacity:0,fillOpacity:0,interactive:false}).addTo(labelsLayer);marker.bindTooltip(x.label,{permanent:true,direction:'center',className:'lumina-road-name',opacity:1});if(used.length>=(map.getZoom()>=16?42:28))break}lastKey=key}catch(e){console.warn('[LUMINA roads]',e)}finally{loading=false;updateCurrentRoad()}};
    const schedule=()=>{clearTimeout(timer);timer=setTimeout(loadRoadNames,260)};map.on('zoomend moveend',schedule);map.whenReady(schedule);const observer=new MutationObserver(()=>setTimeout(updateCurrentRoad,0)),roadEl=document.getElementById('roadName');if(roadEl)observer.observe(roadEl,{childList:true,characterData:true,subtree:true});return map;
  };
  Object.assign(wrappedMap,L.map);wrappedMap.__luminaWrapped=true;L.map=wrappedMap;
})();